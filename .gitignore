# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
*:Zone.Identifier

# accidental shell/scaffold files
/next
/mavya@*

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files
.env*
!.env.local.example

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# scaffold leftover
_scaffold/

# Real customer images used only as LOCAL eval fixtures. Never committed to the
# public repo (privacy). The golden-set entries + customer1-eval.test.ts
# reference these paths, so they must be present locally to run that eval.
eval/fixtures/customer1/

# Scratch file used to pass long commit messages via `git commit -F`. Never
# meant to be tracked; `git add -A` has picked it up by accident before.
COMMIT_MSG.tmp
